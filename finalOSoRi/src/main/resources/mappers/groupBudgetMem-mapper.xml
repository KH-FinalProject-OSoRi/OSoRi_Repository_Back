<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="groupBudgetMem">

<select id="searchGroupMem" parameterType="int" resultType="GroupBudgetMem" >
		SELECT 
            BM.GROUPB_ID AS groupBId,
            BM.USER_ID AS userId,
            BM.ROLE,
            BM.STATUS,
            U.NICKNAME AS nickName
        FROM 
            BUDGETMEM BM
        JOIN 
            USERS U ON BM.USER_ID = U.USER_ID
        WHERE 
            BM.GROUPB_ID = #{groupId}
</select>
	
	

</mapper>